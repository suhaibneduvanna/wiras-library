<section>
    <div class="container mt-5">
        <div class="row" >
            <a href="/admin/add-publisher" class="btn btn-success ml-auto">Add Publisher</a>
        </div>
        <br><br>
        <table id="table" class="table mt-5">
            <thead>
                <tr>
                    
                    <th scope="col">Publisher</th>
                    <th scope="col"></th>
                    
                </tr>
            </thead>
            <tbody>
                {{#each publishers}}
                <tr>
                    
                    <td>{{this.Name}}</td>
                    <td>
                        <a href="/admin/delete-publisher?id={{this._id}}" class="btn btn-danger" onclick="return confirm('Are You Sure to Delete {{this.Name}} ?') ">Delete</a>
                    </td>
                   
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</section>
